'use client';

import React from 'react';

// Simplified for SaaS Look - just a placeholder if needed, mostly handled by body bg
export function Background() {
    return (
        <div className="fixed inset-0 -z-50 pointer-events-none bg-[var(--background)]" />
    );
}
